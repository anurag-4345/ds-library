#include<conio.h>
#include<stdio.h>
int sum(int a)
{
int s ;
if(a==1)
return (a);
s=a+sum(a-1);
return s;
}

void main()
{
int k;
k=sum(5);
printf("sum of the number :- %d ",k);
}
//output (sum of the number :- 15)
